<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mekanik Fiyat Tablosu</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container mt-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2>Fiyat ve Teklif Takip Tablosu</h2>
            <a href="/logout" class="btn btn-danger btn-sm">Sistemden Çık</a>
        </div>

        <div class="card p-3 mb-4 shadow-sm">
            <h5 class="text-primary">Yeni Parça Ekle / Fiyat Güncelle</h5>
            <p class="text-muted small">Not: Aynı firma ve aynı marka ile daha önce eklenen bir parçanın fiyatı otomatik olarak güncellenir.</p>
            <form method="POST" class="row g-2">
                <div class="col-md-3">
                    <input type="text" name="parca_adi" class="form-control" placeholder="Örn: DN 200 SCH40 BORU" required>
                </div>
                <div class="col-md-3">
                    <input type="text" name="firma" class="form-control" placeholder="Tedarikçi Firma" required>
                </div>
                <div class="col-md-3">
                    <input type="text" name="marka" class="form-control" placeholder="Markası" required>
                </div>
                <div class="col-md-2">
                    <input type="number" step="0.01" name="fiyat" class="form-control" placeholder="Fiyat (TL)" required>
                </div>
                <div class="col-md-1">
                    <button type="submit" class="btn btn-success w-100">Kaydet</button>
                </div>
            </form>
        </div>

        <div class="card p-3 shadow-sm">
            <form method="GET" class="mb-3">
                <label class="form-label fw-bold">Sistemde Kayıtlı Parçayı Hızlı Ara:</label>
                <select name="arama" class="form-select border-primary" onchange="this.form.submit()">
                    <option value="">-- Tüm Parçaları Göster --</option>
                    {% for p in parcalar %}
                    <option value="{{ p }}" {% if arama == p %}selected{% endif %}>{{ p }}</option>
                    {% endfor %}
                </select>
            </form>

            <div class="table-responsive mt-2">
                <table class="table table-striped table-hover table-bordered align-middle">
                    <thead class="table-dark">
                        <tr>
                            <th>Parça Adı</th>
                            <th>Tedarikçi Firma</th>
                            <th>Marka</th>
                            <th>Güncel Fiyat</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for teklif in teklifler %}
                        <tr>
                            <td class="text-primary fw-bold">{{ teklif[1] }}</td>
                            <td>{{ teklif[2] }}</td>
                            <td>{{ teklif[3] }}</td>
                            <td class="text-success fw-bold fs-5">{{ teklif[4] }} ₺</td>
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</body>
</html>